version: "2"

linters:
  default: standard
  enable:
    - revive
  settings:
    revive:
      rules:
        - name: exported
          severity: warning

issues:
  exclude-dirs:
    - api/proto/gen
